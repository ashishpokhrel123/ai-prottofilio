{
  "name": "@ai-portfolio/shared",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "echo \"No lint config for shared types\" && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests for shared types\" && exit 0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
