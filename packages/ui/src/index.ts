/**
 * Shared UI primitives (shadcn-style) live here so both the portfolio and the
 * admin console can consume them. Add components as the design system grows.
 */
export const UI_PACKAGE = '@ai-portfolio/ui';
