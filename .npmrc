# pnpm defaults frozen-lockfile=true in CI (e.g. Vercel), which aborts the
# build whenever pnpm-lock.yaml drifts from a package.json. Relax it so a
# dependency change that hasn't had `pnpm install` run + committed still
# installs during deploy instead of failing the build.
frozen-lockfile=false
